%h1 Empresas
%p Listado de empresas

- if @companies.any?
  %table{border: "1"}
    %tr
      %th Name
      %th Address
      %th City
      %th Phone      
    = render @companies
- else
  %em No hay ninguna empresa

%br

= link_to "Nueva empresa", new_company_path